// Re-export from the canonical location (src/gates/index.ts).
// packages/premium imports from here via the workspace package entry point.
export { Feature, checkFeatureAccess } from './gates/index.js';
